import dashboard from './dashboard';
import users from './user';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, users]
};

export default menuItems;
